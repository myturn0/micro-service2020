server:
  port: 9001

spring:
  application:
    name: ms-provider-nacos
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 # nacos地址

management:
  endpoints:
    web:
      exposure:
        include: "*"